export type MailCardType = {
  name: string
  email: string
  number: string
  activity: string
  message: string
  date: string
}

export type MappeCardHeaderType = {
  title: string
  state: string
}
